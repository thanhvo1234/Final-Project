export enum GenderEnum {
    MALE = 'male',
    FEMALE = 'female',
  }
  
  export enum StatusEnum {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
  }
  
  export enum RoleEnum {
    STORE_OWNER = 'admin',
    CUSTOMER = 'user',
  }
  
  export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
  }